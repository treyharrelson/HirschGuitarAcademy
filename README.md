# HirshGuitarAcademy
# All files for the node js server have been pushed.
# You must have Node.js installed for it to function
# In the directory of the main folder, open a command line (windows cmd or git bash)
# you can launch the webpage using the "node app.js" command
# it will launch the server on localhost using port 3000
# you can enter "localhost:3000" into your browser with ther server active to view the "index.html" page
# Note, the login system has no functionality at the moment and will not work properly 